services:
  - type: web
    name: rotiseria-chatbot
    env: python
    buildCommand: ""
    startCommand: gunicorn app:app
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: rotiseria-db
          property: connectionString
databases:
  - name: rotiseria-db
    plan: free
